---
title: Task_1
date: 2020-03-12 15:00:00 +0900
categories: [Grind, C#]
tags: [c#]
seo:
  date_modified: 2020-03-14 10:12:43 +0900
---

# Build Up 
> Action/Func - ***Task*** - async/await

## 설명
> Represents an asynchronous operation.  

> The Task class represents a single operation that does not return a value and that usually executes asynchronously. Task objects are one of the central components of the task-based asynchronous pattern first introduced in the .NET Framework 4. Because the work performed by a Task object typically executes asynchronously on a thread pool thread rather than synchronously on the main application thread, you can use the Status property, as well as the IsCanceled, IsCompleted, and IsFaulted properties, to determine the state of a task. Most commonly, a lambda expression is used to specify the work that the task is to perform.
For operations that return values, you use the Task\<TResult\> class.

- 비동기 동작을 나타냄.
- Task는 return 없이 하는 비동기 작업을 나타낸다 하는데 represents a single operation 을 굳이 넣은거보니 중요한 말인가? 단일? 단위? 작업임.  
return을 받고싶으면 Task\<TResult\>사용.
- 작업기반 비동기 패턴의 중심 구성요소라 함 build up에 순서가 있지만 원래 async키워드를 쓰려는 과정에서 이전 요소들을 확실히 쓰고 가고싶어서 이지경이 됐음.
- main thread에 동기적 실행이 아니라 thread pool에서 비 동기적으로 실행함 그러니까 Task만나면 분기되서 따로 실행..한다하면 되나 이게 암튼 주요 사용 이유겠지
- 그 아래는 취소, 완료, 실패 이런거 상태볼수있음, 람다식 사용이 일반적임 이런 설명인데 이건좀 나중에

## 사용
- 근본
    ```c#
    class Task_1
    {
        public Task_1()
        {
            Action act = mtd;

            Task t = new Task(act);
            t.Start();
            //t.Wait();
        }

        void mtd()
        {
            Console.WriteLine("task_1");
        }
    }
    ```
    - 이렇게하면 암껏도 안하고 끝난다. 이유는 t.start(); 하고 main은 그대로 진행, task는 따로 진행해서 그런가봄.(비동기니까)  
    t.wait();를 해야 mtd()까지 실행 한 결과를 볼 수 있다.(저 주석만 풀면 됨.)  
    별 다른 일 없으면 짝이라 보자
- 다른 방법
    ```c#
    class Task_2
    {
        public Task_2()
        {
            mtd_1();
        }

        void mtd_1()
        {
            Action act_1 = () => { Console.WriteLine("mtd_1"); };
            Task t_1 = Task.Factory.StartNew(act_1);
            t_1.Wait();

            Action<object> act_2 = (object s1) => { Console.WriteLine("{0}", s1); };
            Task t_2 = new Task(act_2, "1");
            t_2.Start();
            t_2.Wait();

            Task t_3 = Task.Run(() => Console.WriteLine("use run"));
            t_3.Wait();

        }
    }
    ```
    - t_1, t_2, t_3 각각 조금 다르게 쓰는 방법들임.
        - t_1
            - 원래 action에서 method를 연결 하는 대신 저렇게 간단하게 쓰고 이 방법이 처음 설명에서 말하는 람다식 사용해 표현하는 방법인데 이게 무명메서드인가...
            - 원래 Task.Factory.StartNew(act_1); 요부분을 설명하려던건데 이건 바로 실행임 .start()가 필요없음
        - t_2
            - parameter를 줘야 할때. 
            - 하나밖에 안됨.. Action<obj, obj> 이런식은 못받아줌 Task parameter에 object로 받는거 state 하나밖에 없음.  
            obj라 여러개여도 상관없긴함.
        - t_3
            - parameter없고 return 없으면 사용할수 있는 방법
            - 이것도 바로 실행
- 하나 더 
    ```c#
    class Task_3
    {
        public Task_3()
        {
            Task t = new Task(() => { Console.WriteLine("sync"); });
            t.RunSynchronously();
        }
    }
    ```
    - Task t4 is executed synchronously on the main thread by calling the RunSynchronously() method.
    - Although the task was run synchronously, it is a good practice to wait for it in the event exceptions were thrown by the task.
    - 원래 방식으로 main thread에서 실행함.
        - 예를들어 위 t_3.wait를 빼고 Task_2실행했던 부분 다음에 Task_3 실행 하면 t_3정상적으로 끝남(당연히 t_3은 실행시 지나쳤고 다음 Task_3은 Main에서 실행 했으니...)
    - 두번째가 뭔소린지 모르겠음

## 결론
- 설명처럼 비동기 작업을 함
- Action만 해주고 delegate는 안되는줄알았는데 
    - 이렇게 하면 가능
    ```c#
        Task tt = new Task(delegate
        {
            Console.WriteLine("delegate");
        });
        tt.Start();
        tt.Wait();
    ```
    - 근데 delegate를 ()=> 로 바꾸면 의미가 같아지는것같음
    - 다른 delegate형은 안됨 예를들어
    ```c#
        delegate void dele();
        ///    
        dele del = mtd;
        Task ttt = new Task(del);
    ```
- 간랸히 쓰는 형태가 많다. 
- 근데 좀 궁금한 부분들이 더 있어서 실행 상태보는 부분이랑 취소 등등 해서 다음꺼에 더 쓸라고.

## 참고
- 더 좋은 설명들  
[Task Class](https://docs.microsoft.com/en-us/dotnet/api/system.threading.tasks.task?view=netframework-4.8)  
[Task](https://referencesource.microsoft.com/#mscorlib/system/threading/Tasks/Task.cs,045a746eb48cbaa9)  
[Task 클래스](http://www.csharpstudy.com/Threads/task.aspx)
- test
    ```c#
    class Program
    {
        static void Main(string[] args)
        {
            new Task_1().t.Wait();
            new Task_2();
            new Task_3();
        }
    }

    class Task_1
    {
        public Action act;
        public Task t;
        public Task_1()
        {
            act = mtd;

            t = new Task(act);
            t.Start();
            //t.Wait();
        }

        void mtd()
        {
            Console.WriteLine("task_1");
        }
    }
    class Task_2
    {
        public Task_2()
        {
            mtd_1();
        }

        void mtd_1()
        {
            Action act_1 = () => { Console.WriteLine("mtd_1"); };
            Task t_1 = Task.Factory.StartNew(act_1);
            t_1.Wait();

            Action<object> act_2 = (object s1) => { Console.WriteLine("{0}", s1); };
            Task t_2 = new Task(act_2, "1");
            t_2.Start();
            t_2.Wait();

            Task t_3 = Task.Run(() => Console.WriteLine("use run"));
            //t_3.Wait();

        }
    }
    class Task_3
    {
        public Task_3()
        {
            Task t = new Task(() => { Console.WriteLine("sync"); });
            t.RunSynchronously();
        }
    } 
    ```